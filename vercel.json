{
  "installCommand": "pnpm install",
  "buildCommand": "cd apps/web && npx tsc && npx vite build",
  "framework": null,
  "outputDirectory": "apps/web/dist",
  "rewrites": [
    {
      "source": "/((?!assets/).*)",
      "destination": "/index.html"
    }
  ]
}