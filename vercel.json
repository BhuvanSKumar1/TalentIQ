{
  "installCommand": "pnpm install",
  "buildCommand": "pnpm --filter web exec vite build",
  "framework": null,
  "outputDirectory": "apps/web/dist",
  "rewrites": [
    {
      "source": "/((?!assets/).*)",
      "destination": "/index.html"
    }
  ]
}